import 'package:flutter/material.dart';

AppBarTheme appBarTheme({required Color color, required Color foregroundColor}) {
  return AppBarTheme(
    color: color,
    foregroundColor: foregroundColor,
  );
}
